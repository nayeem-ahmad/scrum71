version: '3.8'
services:
  scrum71:
    build: .
    container_name: scrum71
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - shared_vps

networks:
  shared_vps:
    external: true
    name: retail-saas_default
